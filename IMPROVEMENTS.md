# 現状の課題と改善計画

ポートフォリオ・実運用の両観点で見つかった課題を、優先度順にまとめる。各項目は「課題 → 影響 → 修正法」の形式で記載する。

## 優先度: 高

### 1. Before/After画像が無い

- **課題**: READMEに変換前後の画像が1枚も無く、テキストのみで説明している。
- **影響**: このツールの価値は「見た目の変換品質」そのものなので、画像なしでは一番伝わるべき情報が伝わらない。閲覧者が実際の出力を想像できない。
- **修正法**: `scripts/real_sample_chibi.png` / `scripts/real_sample_standing.png` / `scripts/real_sample_fox_doctor.png` など既存のサンプル画像を使い、`grid_size=32/64/128` それぞれの変換結果を並べた比較画像を作成し、READMEの冒頭に埋め込む。

### 2. `requirements.txt` にバージョン固定が無い

- **課題**: `fastapi` / `Pillow` / `numpy` などが全てバージョン無指定。
- **影響**: 依存ライブラリの将来のメジャーアップデートで動作が壊れるリスクがあり、再現性が担保されていない。
- **修正法**: 現在の稼働環境で `pip freeze > requirements.lock.txt` を実行し、最低限メジャーバージョンだけでも `fastapi>=0.110,<1.0` のように固定する。

### 3. ドキュメントの記述が実装状況と食い違っている

- **課題**: `docs/design/00-overview.md` に「実装状況: 設計のみ（未実装）」という記述が残っているが、実際にはAPI・パイプラインとも実装済み。
- **影響**: ドキュメントの鮮度が保たれていない印象を与える。
- **修正法**: 実装完了時点で該当行を「実装済み・動作確認済み」に更新する。今後は機能追加のたびにこの欄を更新する運用にする。

### 4. 自動テスト・CIが無い

- **課題**: `scripts/test_pipeline.py` は手動実行前提の smoke test であり、pytestベースの自動テストやCIパイプラインが存在しない。
- **影響**: 変更が既存の動作を壊していないことを客観的に保証できない。特にこのプロジェクトは `CLAUDE.md` で「grid_size(32/64/128) × colors(8/10) × output_size(64/128) の12パターン全確認」を必須ルールにしているが、それが自動化されていない。
- **修正法**: `pytest` を導入し、サンプル画像をfixtureとしてリポジトリに含める。12パターンを `@pytest.mark.parametrize` で網羅するテストを書き、GitHub Actionsで push・PR時に自動実行する。これにより手動確認のルールをコードで担保できる。

## 優先度: 中

### 5. LICENSEファイルが無い

- **課題**: リポジトリにライセンス表記が無い。
- **影響**: 第三者がコードを参照・再利用してよいか判断できない。
- **修正法**: MITライセンス等をリポジトリルートに追加する。

### 6. コミット履歴が1コミットのみ

- **課題**: 開発の全過程を1コミットにまとめて公開したため、設計→実装→修正の過程が見えない。
- **影響**: 実際の開発プロセス（試行錯誤・設計判断）が伝わらず、完成品をそのまま貼り付けたように見える。
- **修正法**: 今後の機能追加・バグ修正は小さい単位でコミットし、コミットメッセージに「なぜ」を書く。`docs/design/` 配下の更新履歴を参照しながら、主要な設計判断を `CHANGELOG.md` として遡って書き起こすのも一案。

### 7. `output_size=64` かつ `grid_size=128` のような不整合な組み合わせのハンドリングが未文書化

- **課題**: `output_size` が `grid_size` より小さい設定（例: grid_size=128, output_size=64）の挙動が README/docs 双方に明記されていない。
- **影響**: 利用者が意図しない出力（過度な縮小によるディテール消失）に遭遇する可能性がある。
- **修正法**: `docs/design/04-constraints-and-limitations.md` に組み合わせごとの推奨・非推奨パターンを明記し、UIにも警告表示を追加する。
